<form method="post" action="{{ path('app_attendee_delete', {'id': attendee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attendee.id) }}">
    <button class="btn">Delete</button>
</form>
